"use strict";

const string = "sol";
for (const char of string) {
    console.log(`string - ${char}`);
}

const array = ["s", "o", "l"];
for (const char of array) {
    console.log(`array - ${char}`);
}
