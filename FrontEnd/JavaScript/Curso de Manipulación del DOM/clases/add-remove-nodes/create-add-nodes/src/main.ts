import "./style.css";

// Referencies
const mainNode = document.getElementById("list");

// Nodes
// * MODO DE CREAR ELEMENTOS
// const node = document.createElement("li");
// const textNode = document.createTextNode("Hello World");

// node.append(textNode);

// Adding nodes to the reference
// reference?.append(node);

// * MODOS DE CREAR NODOS (Ver otros archivos para ver ejemplos)
// 1. Append (Método Nuevo) => Agrega varios nodos al final de la lista -- No Compatible con
// 2. AppendChild (Método Viejo) => Agrega un nodo al final de la lista
// 3. InsertBefore => Agrega un nodo antes de otro nodo
// 4. insertAdjacentElement (Método muy flexible) => Agrega un nodo antes o después de otro nodo

export default mainNode;
