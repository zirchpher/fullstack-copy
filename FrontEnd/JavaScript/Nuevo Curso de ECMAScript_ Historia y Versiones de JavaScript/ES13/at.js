// At nos permite acceder al último elemento por su índice
const numbers = ["one", "two", "three", "four", "five", "six", "seven"];

// const lastNumber = numbers[numbers.length - 1];
const lastNumber = numbers.at(0);
console.log(lastNumber);