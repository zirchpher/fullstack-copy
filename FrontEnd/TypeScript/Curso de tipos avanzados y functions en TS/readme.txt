Ahora con ts-node vamos a poder correr código TS direactamente sin crear manualmnente archivos TS => JS => run node src/archivo.js
sino que lo haremos directamente

1) instalamos la librería:
$ npm install -D ts-node

2) Ejecuta un archivo
$ npx ts-node [src/index.ts]